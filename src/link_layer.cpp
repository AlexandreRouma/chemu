#include "link_layer.h"

namespace CODAN {
    int LinkLayer::send(uint8_t phyAddr, uint8_t linkAddr, uint8_t* data, int len) {

    }

    void LinkLayer::worker() {
        while (true) {
            
        }
    }
}